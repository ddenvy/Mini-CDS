<Window x:Class="MiniCds.Wpf.Views.ReportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:MiniCds.Wpf.Converters"
        mc:Ignorable="d"
        Title="Export Report" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Window.Resources>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Report Title" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding ReportTitle, UpdateSourceTrigger=PropertyChanged}"
                     IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                     Height="28" Padding="5,3"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,15" Orientation="Horizontal">
            <TextBlock Text="Format" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding AvailableFormats}"
                      SelectedItem="{Binding SelectedFormat}"
                      IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                      Width="120" Height="28"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Select Samples" Margin="0,0,0,15">
            <Grid>
                <ListBox ItemsSource="{Binding AvailableSamples}"
                         IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                         Margin="5">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}"
                                      Content="{Binding DisplayName}"
                                      Margin="2"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <TextBlock Text="Loading samples..."
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           Foreground="Gray"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <TextBlock Text="{Binding AvailableSamples.Count, StringFormat='Selected: {0} samples'}"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"
                       Foreground="Gray"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" 
                    Command="{Binding OkCommand}"
                    IsDefault="True"
                    Width="80" Height="30"
                    Margin="0,0,10,0"/>
            <Button Content="Cancel" 
                    Command="{Binding CancelCommand}"
                    IsCancel="True"
                    Width="80" Height="30"/>
        </StackPanel>
    </Grid>
</Window>

<UserControl x:Class="MiniCds.Wpf.Views.PeaksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="800">
    <DataGrid ItemsSource="{Binding DetectedPeaks}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              HeadersVisibility="Column"
              GridLinesVisibility="Horizontal"
              AlternatingRowBackground="#FFF5F5F5"
              AlternationCount="9999"
              SelectionMode="Single">
        <DataGrid.Columns>
            <DataGridTextColumn Header="#"
                                Binding="{Binding (ItemsControl.AlternationIndex),
                                                  RelativeSource={RelativeSource AncestorType=DataGridRow},
                                                  StringFormat={}{0}}"
                                Width="40"/>
            <DataGridTextColumn Header="RT (s)"
                                Binding="{Binding Metrics.RetentionTime, StringFormat=F3}"
                                Width="*"/>
            <DataGridTextColumn Header="Height"
                                Binding="{Binding Metrics.Height, StringFormat=F2}"
                                Width="*"/>
            <DataGridTextColumn Header="Area"
                                Binding="{Binding Metrics.Area, StringFormat=F2}"
                                Width="*"/>
            <DataGridTextColumn Header="FWHM (s)"
                                Binding="{Binding Metrics.Fwhm, StringFormat=F3}"
                                Width="*"/>
            <DataGridTextColumn Header="Plates"
                                Binding="{Binding Metrics.Plates, StringFormat=F0}"
                                Width="*"/>
            <DataGridTextColumn Header="Tailing"
                                Binding="{Binding Metrics.Tailing, StringFormat=F2}"
                                Width="*"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
